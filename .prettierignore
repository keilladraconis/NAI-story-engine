# Generated by nibs from NovelAI's published definitions, plus vendored
# reference material. None of it is authored here, and `npm run format`
# otherwise rewrites the typings wholesale — thousands of lines of phantom
# diff that bury the real change.
external/

# Build output.
dist/

# Vendored upstream skills (scripts/vendor_superpowers.sh). Same reasoning as
# external/: not authored here, and formatting them means every re-vendor
# fights the formatter over ~900 lines that are not ours to restyle.
.claude/skills/
